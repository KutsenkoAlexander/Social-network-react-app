import React from 'react';
import spinner from '../../../assets/img/spinner.svg';

const Preloader = (props) => {
    return <div><img alt={'Preloader'} src={spinner}/></div>
}

export default Preloader;