import React from 'react';
import LoginContainer from "./LoginContainer";

const Login = (props) => {
    return (
        <div>
            <h1>Login Page</h1>
            <LoginContainer/>
        </div>
    );
}

export default Login;